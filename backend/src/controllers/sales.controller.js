// const sales = require('../services/sales.service');
// const mapStatusHTTP = require('../utils/mapStatusRest');

// const createSales = async (req, res) => {
//   const { saleId } = req.params;
//   const travelData = { passengerId, ...req.body };

//   const { status, data } = await sales.requestTravel(travelData);
//   return res.status(mapStatusHTTP(status)).json(data);
// };

// module.exports = {
//   createTravel,
// };